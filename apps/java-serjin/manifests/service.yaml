apiVersion: v1
kind: Service
metadata:
  name: java-serjin
  namespace: java-serjin
  labels:
    app.kubernetes.io/name: java-serjin
spec:
  selector:
    app.kubernetes.io/name: java-serjin
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
