apiVersion: v1
kind: Service
metadata:
  name: node-express
  namespace: node-express
  labels:
    app.kubernetes.io/name: node-express
spec:
  selector:
    app.kubernetes.io/name: node-express
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
  type: ClusterIP
