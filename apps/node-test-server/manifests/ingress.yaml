apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: node-test-server
  namespace: node-test-server
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: nginx
  rules:
    - host: node-test-server.sesac.already11.cloud
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: node-test-server
                port:
                  number: 80
  tls:
    - hosts:
        - node-test-server.sesac.already11.cloud
      secretName: node-test-server-tls
