apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: java-server
  namespace: java-server
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: nginx
  rules:
    - host: java-server.sesac.already11.cloud
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: java-server
                port:
                  number: 80
  tls:
    - hosts:
        - java-server.sesac.already11.cloud
      secretName: java-server-tls
