apiVersion: v1
kind: Service
metadata:
  name: java-server
  namespace: java-server
  labels:
    app.kubernetes.io/name: java-server
spec:
  selector:
    app.kubernetes.io/name: java-server
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
