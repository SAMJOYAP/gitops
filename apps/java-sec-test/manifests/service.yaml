apiVersion: v1
kind: Service
metadata:
  name: java-sec-test
  namespace: java-sec-test
  labels:
    app.kubernetes.io/name: java-sec-test
spec:
  selector:
    app.kubernetes.io/name: java-sec-test
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
