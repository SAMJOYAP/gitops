apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: java-sec-test
  namespace: java-sec-test
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: nginx
  rules:
    - host: java-sec-test.sesac.already11.cloud
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: java-sec-test
                port:
                  number: 80
  tls:
    - hosts:
        - java-sec-test.sesac.already11.cloud
      secretName: java-sec-test-tls
