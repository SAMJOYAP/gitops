apiVersion: apps/v1
kind: Deployment
metadata:
  name: node-test
  namespace: node-test
  labels:
    app.kubernetes.io/name: node-test
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: node-test
  template:
    metadata:
      labels:
        app.kubernetes.io/name: node-test
    spec:
      containers:
        - name: app
          image: 710232982381.dkr.ecr.ap-northeast-2.amazonaws.com/node-test:1.0.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
