apiVersion: v1
kind: Service
metadata:
  name: node-test
  namespace: node-test
  labels:
    app.kubernetes.io/name: node-test
spec:
  selector:
    app.kubernetes.io/name: node-test
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
  type: ClusterIP
