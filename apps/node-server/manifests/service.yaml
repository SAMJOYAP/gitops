apiVersion: v1
kind: Service
metadata:
  name: node-server
  namespace: node-server
  labels:
    app.kubernetes.io/name: node-server
spec:
  selector:
    app.kubernetes.io/name: node-server
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
  type: ClusterIP
