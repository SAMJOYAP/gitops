apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: java-send
  namespace: java-send
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: nginx
  rules:
    - host: java-send.sesac.already11.cloud
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: java-send
                port:
                  number: 80
  tls:
    - hosts:
        - java-send.sesac.already11.cloud
      secretName: java-send-tls
