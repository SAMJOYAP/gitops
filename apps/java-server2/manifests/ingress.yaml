apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: java-server2
  namespace: java-server2
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: nginx
  rules:
    - host: java-server2.sesac.already11.cloud
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: java-server2
                port:
                  number: 80
  tls:
    - hosts:
        - java-server2.sesac.already11.cloud
      secretName: java-server2-tls
