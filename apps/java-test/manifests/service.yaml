apiVersion: v1
kind: Service
metadata:
  name: java-test
  namespace: java-test
  labels:
    app.kubernetes.io/name: java-test
spec:
  selector:
    app.kubernetes.io/name: java-test
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
