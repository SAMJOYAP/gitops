apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: java-test
  namespace: java-test
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: nginx
  rules:
    - host: java-test.sesac.already11.cloud
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: java-test
                port:
                  number: 80
  tls:
    - hosts:
        - java-test.sesac.already11.cloud
      secretName: java-test-tls
